import React from 'react';
import QuizInstructions from './QuizInstructions';
import { Link } from 'react-router-dom';
const root = ReactDOM.createRoot(document.getElementById('bit'));
root.render(
  <React.StrictMode>
    <QuizInstructions />
  </React.StrictMode>
);

